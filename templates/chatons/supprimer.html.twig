{% extends 'base.html.twig' %}

{% block title %}Supprimer le chaton{% endblock %}

{% block body %}
    <div class="container">
        <h1>Supprimer un chaton <i>{{ chaton.nom }}</i></h1>
        {{ form(formulaire) }}
        <a href="{{ path("app_categories") }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}